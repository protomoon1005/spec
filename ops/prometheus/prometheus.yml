# TODO(9단계): api /metrics 가 실제로 뜨기 전까지는 scrape target이 항상 down으로 보인다.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: api
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
