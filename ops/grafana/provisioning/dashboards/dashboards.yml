# json/ 아래 대시보드 JSON을 자동으로 불러오는 provider 설정.
apiVersion: 1

providers:
  - name: spec-dashboards
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
