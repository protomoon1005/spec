# M1 전략 컴파일 — 마일스톤

- 재원 담당. 단계별 목표와 **"끝났다"고 말할 수 있는 기준**을 적는다
- 알려진 설계 구멍은 `docs/known-issues.md`
- 작성 2026-09-20. **`main` 병합 이후 기준으로 고쳐 둠** — 병합 전 수치(종목 173 등)가
  남아 있는 곳은 그 시점 기록이라는 표시를 달았다

---

## 목표

- 사용자가 쓴 문장을 **전략서(Spec JSON)** 로 바꾸고, 그 결과를 데이터베이스에 남기는 것까지
- 전략서가 지켜야 할 범위(성향별 종목 상한)는 **만들 때부터 박아 넣는다**
  - 나중에 검사해서 걸러내는 게 아니라, 애초에 어길 수 없는 스키마를 LLM 에 준다

### M1 이 끝났다고 말할 수 있는 조건

- 자연어 한 줄을 넣으면 → 성향에 맞는 전략서가 나오고 → 데이터베이스에 저장된다
- 샘플 10건이 전부 통과한다
- 같은 입력을 두 번 넣으면 같은 후보 종목과 같은 허용범위가 나온다
  - LLM 출력 자체는 흔들릴 수 있지만, **후보 선정과 범위 계산은 흔들리면 안 된다**

---

## 0단계 — 바닥 (완료, 2026-09-20)

- 표 구조 36개 생성. `db/init/` 이 안 돌던 문제를 볼륨 재생성으로 해결
- 기준표 적재 — 공통 상한선 1 · 프리셋 30(5×6) · 묶음 상한 25 · 종목 173
  (이때의 원장. 병합으로 **71종목**으로 교체됐다)
- 종목 → 등급 → 성향별 상한 조회가 실제로 이어지는 것 확인
- `POST /auth/signup` 추가. Swagger 에서 계정을 만들어 바로 토큰을 받는다
- Swagger 설명 전면 한글화. 토큰 만료 30일로 늘려 둠

---

## 합병으로 풀린 것 (2026-09-20, `main` 병합)

다른 라인의 작업이 들어오면서 **선행 안건 ① 과 ⑤, 국가 칸 문제가 해소됐다.**

- **위험등급 재배정 완료** — 종목 원장이 71종목으로 손질돼 들어왔다
  - G1 이 87개에서 **14개**로. KODEX 200 이 G3 가 되어 위험중립형이 담을 수 있다
  - `db/seeds/04_etf_master.csv` 가 정본이고 `scripts/load_etf_master.py` 가 적재한다
- **국가 칸 생김** — `db/migrate/001_etf_master_group_axes.sql` 이 분류를 세 축으로 나눴다
  (`asset_group_id` · `sector_group_id` · `country_group_id`)
- **레버리지·상장폐지 종목이 실제 종목으로 원장에 있다** — 레버리지 15개, 상장폐지 3개.
  지어냈던 `DEMO-001` 은 필요 없어졌다
- **마이그레이션 경로가 생겼다** (`scripts/apply_migrations.py`) — 스키마를 고칠 때
  볼륨을 날리지 않아도 된다. 앞서 "Alembic 을 되살릴지" 로 논의하던 문제의 답

**M1 쪽에서 고친 것**: 분류를 읽는 자리를 옛 `sector` 컬럼에서 `sector_group_id` 로 옮겼다.
옛 컬럼에는 한글 라벨(`반도체`)이 들어 있어서 **섞어 읽으면 조용히 아무것도 안 걸린다.**
실제로 그렇게 깨졌다가 테스트가 잡았다. 원장이 통째로 바뀌었는데 깨진 테스트가 둘뿐이었던
것은 **테스트에 특정 종목의 등급을 박지 않았기 때문**이다.

**새로 드러난 것** (`docs/known-issues.md`)

- 성향 1 이 담을 수 있는 **주식형이 0종목** — 원장에 저위험 주식형이 없다
- **성향 3·4·5 의 선택지가 똑같다(54종목)** — G1 14개가 전부 레버리지라 하드캡이 먼저 막는다.
  성향을 올려도 안 늘어나서 데모에서 성향 차이를 보이기 어렵다
- **뉴스 수집 파이프라인이 제거됐다** (`jun`, 커밋 `ee08952`). 감성 관점 입력이 통째로 없다

---

## 선행 안건 — 결정 결과 (2026-09-20 팀 상의)

### ① 위험등급 재배정 — **해소됨** (병합, 위 절 참고)

- **문제였던 것**: 등급이 3년 변동성으로 자동 산정됐고 구간 경계가 팀 임의값이었다.
  산정 방식(3년 일간수익률)은 협회 규칙대로인데 "몇 %부터 G1인가" 를 지어냈다.
  그래서 173종목 중 87개가 G1 에 쏠렸고 KODEX 200 도 G1 이라 위험중립형조차
  국내 대표지수를 못 담았다
- **스킵하고 진행하기로 했고**, 그동안 테스트에 실제 종목의 등급을 박지 않았다

### ② 되묻기 대화 상태 — **Redis. 메모리는 안 된다**

- 메모리(파이썬 변수)가 안 되는 이유: **프로세스가 다르다**
  - 컴파일은 Celery 작업이라 `worker` 컨테이너에서 돈다
  - 사용자 응답은 `api` 컨테이너로 들어온다
  - 되묻기는 "worker 가 묻고 → 사용자가 api 로 답하고 → worker 가 이어받는" 왕복이라
    정확히 여기서 막힌다. 거기다 `--reload` 라 코드를 고칠 때마다 날아간다
- Redis 가 원하던 장점을 그대로 가져간다
  - **표를 추가하지 않는다** — 전원 데이터베이스 날릴 일 없음
  - 이미 떠 있고(Celery 가 쓰는 중) 패키지도 깔려 있다(헬스체크가 쓴다)
  - 프로세스가 달라도 공유된다. 만료를 걸어 버려진 대화가 쌓이지 않게 한다

### ③ 비중 클램프 — **Validator 담당으로 확정**

- 경계가 이렇게 나뉜다
  - **M1** — 프리셋을 스키마에 박아 넣어 **애초에 못 어기게** 한다
  - **Validator** — 하드캡으로 **접는다.** 접었다는 사실(`was_adjusted`)도 여기서 남긴다
- M1 은 5단계에서 파싱과 저장까지만 한다. 원출력과 확정값이 같고 `was_adjusted=false` 로 들어간다
- **남은 구멍: Validator 본체가 아직 없다.** 인터페이스조차 없다.
  데모까지 안 나오면 **하드캡을 넘는 전략서가 그대로 저장된다.** 언제 누가 만들지 확인할 것

### ④ 파이프라인 라이브러리 — **M1 전용 묶음으로**

- **다만 이미지 분리가 아니다.** api·worker·beat 가 Dockerfile 하나를 공유하고
  팀이 안 쪼개기로 했다. `.[dev,m1]` 로 깔면 세 컨테이너에 전부 들어간다
- 얻는 것은 "안 깔고도 앱이 뜬다" 와 "CI 는 `.[dev]` 만 깔아 M1 테스트를 마커로 뺀다"
- **격리의 실체는 의존성보다 디렉터리와 접점**이다. `app/m1/` 한 곳에 몰고
  태스크 함수 안에서 import 한다

### ⑤ 거절 장면용 종목 — **해소됨** (병합)

- 한때 직접 시드에 넣었다가 지웠다. 수집 스크립트가 일부러 걸러내는 종목이라 원장에
  상주할 이유가 없었고, 상장폐지 종목은 확인할 경로가 없어 **지어낸 값**이었다
- 병합된 원장에 실제 종목이 들어 있다
- 막는 규칙의 테스트는 `tests/conftest.py` 의 `blocked_etfs` 픽스처를 쓴다.
  **테스트 데이터는 테스트 안에 두는 편**이 낫다 — 원장이 또 바뀌어도 안 깨진다

### 아직 안 정한 것

- **감성 렌즈** (`jun`, M3) — BBL 의 렌즈 블록을 비워 뒀다. 수집 파이프라인까지 제거돼
  감성 관점 입력이 통째로 없다. 다시 만들 계획인지 확인 필요
- **국내/해외 필터 블록** — 국가 칸이 생겨서 이제 만들 수 있다. BBL 에 아직 없다
- **정책 수치 사본 통일** (`paekh`) — 지금은 어긋나면 테스트가 잡게 해 뒀다

---

## 1단계 — 성향 판정

- **하는 일** (`m1-todo` §2)
  - 설문 문항과 점수 매핑 정의
  - 점수 합산 → 성향 1~5 산출
  - `POST /profile/survey` · `POST /profile` · `GET /profile/me` 본체 채우기
- **재사용할 것**
  - 라우터 3개의 경로·응답 형식·인증이 이미 열려 있음. **본체만 채운다**
  - 성향별 현금 하한·라벨은 `backtest/policy.py` 에 이미 있음. 다시 정의하지 말 것
- **완료 기준**
  - Swagger 에서 설문 제출 → 성향 확정 → `GET /profile/me` 가 등급을 돌려줌
  - `risk_profiles` 에 행이 생기고, 어떤 기준표 버전으로 정했는지 같이 기록됨
  - 같은 답을 넣으면 항상 같은 등급이 나옴
- **막는 것 없음. 여기부터 시작한다**

## 2단계 — 프리셋 조회기

- **하는 일** (`m1-todo` §3)
  - (성향, 종목등급) → 허용 상한 조회
  - 성향 → 자산군·업종·국가 상한 조회
- **핵심 — 새로 만드는 게 아니라 옮기는 것**
  - 같은 수치가 이미 세 군데 있음: 시드 SQL · `backtest/policy.py` · 프론트
  - `policy.py` 머리에 *"최종적으로는 DB 에서 읽어야 한다. 그때는 이 파일의 상수만 조회 함수로 바꾸면 된다"* 고 적혀 있음
  - **함수 이름과 반환 모양을 기존 것에 맞추면 백테스트 호출부를 안 건드려도 된다**
- **완료 기준**
  - 조회 함수가 DB 를 읽고, 시드 수치와 한 칸도 틀리지 않음
  - `policy.py` 의 상수가 사라지고 사본이 3벌 → 2벌로 줄어듦
  - 백테스트가 예전과 같은 결과를 냄 (두 러너 대조 통과)
- **선행 안건 ① 이 끝나야 결과가 의미를 가짐.** 코드는 먼저 짜도 됨

## 3단계 — BBL (블록 라이브러리)

### BBL 이 실제로 무엇인가

블록 라이브러리는 **전략서에 들어갈 수 있는 값들의 목록**이다.

전략서 계약(`contracts/spec.py`)에 `# TODO: 값 집합은 BBL 확정 후 M1` 주석이 일곱 군데
달려 있다. 자리와 타입은 고정됐는데 **어떤 값이 허용되는지가 비어 있다.**
그 빈칸을 채우는 것이 3단계다.

- `rebalance.trigger.type` · `freq` · `day` — 지금은 아무 문자열이나 정수
  - 리밸런싱 블록이 허용값을 정한다
- `signal_rules.market_analysis.indicators[]` — 지금은 아무 문자열
  - 지표 블록 9개가 허용값을 정한다
- `signal_rules.sentiment.target_sectors[]` — 지금은 아무 문자열
  - 업종 묶음 7개가 허용값을 정한다
- `signal_rules.market_temperature.trend_index` · `volatility_index` — 지금은 아무 문자열
  - 거시지표 블록이 허용값을 정한다

**즉 BBL 은 "자연어를 무엇으로 번역할 수 있는가" 의 사전이다.** 사전에 없는 말은
전략서에 못 들어간다.

### 블록 식별자 규약

- **지표 블록은 식별자가 곧 전략서에 들어갈 값이다.** `ret_20` 블록을 고르면
  `indicators` 에 `"ret_20"` 이 그대로 들어간다. 번역표를 따로 두지 않는다
- 나머지는 접두어를 붙인다 — `RB_`(리밸런싱) · `FL_`(필터) · `ET_`(ETF 특성)
- 이유: 지표는 이미 `features.py` 가 이름을 정해 뒀고, 그 이름이 판단 계층이
  실제로 쓰는 이름이다. 다른 이름을 붙이면 변환이 한 겹 생기고 언젠가 어긋난다

### 블록 종류별 시드 (다섯 중 넷만 채운다)

**① 지표 `indicator` — 13개**

- 가격 지표 9개는 `features.py` 의 피처셋 `v0.1-ta9` 그대로
  `ret_1` · `ret_5` · `ret_20` · `ma_gap_20` · `ma_gap_60` · `rsi_14` · `atr_14_pct` ·
  `vol_20` · `volume_ratio_20`
- 거시지표 4개는 `macro/codes.py` 에 실제로 수집 중인 것만
  `KOSPI200` · `VIX_CLOSE` · `USDKRW` · `CREDIT_SPREAD_BAA10Y`
- **없는 이름을 넣지 말 것.** 예시 파일에 있던 `momentum_20d` · `VKOSPI` 는
  계산하지도 수집하지도 않는다. 넣으면 판단 계층이 그 값을 못 찾는다
- 지표를 늘리려면 피처셋 버전을 올려야 하고 그건 M3 영역이다

**② 리밸런싱 `rebalance` — 6개**

- `RB_MONTHLY_FIRST` 월 첫 거래일 · `RB_MONTHLY_MID` 월 중순
- `RB_QUARTERLY` 분기 첫 거래일 · `RB_WEEKLY` 주 1회
- `RB_THRESHOLD_5` 비중이 5%p 벗어나면 · `RB_SIGNAL` 신호가 바뀌면
- 각 블록의 `params_schema` 에 전략서에 박힐 모양을 그대로 담는다
  예: `{"type": "calendar", "freq": "monthly", "day": 1}`
- **하드캡의 최소 간격(5일)보다 짧은 주기는 넣지 않는다.** 넣어 봐야 나중에 접힌다

**③ 필터 `filter` — 8개 내외**

- 후보 종목을 추리는 조건. 자연어의 "~만" / "~빼고" 에 대응한다
- 예: `FL_NO_LEVERAGE` 레버리지·인버스 제외 · `FL_DOMESTIC_ONLY` 국내만 ·
  `FL_BOND_ONLY` 채권만 · `FL_LARGE_ONLY` 순자산 상위만
- `params_schema` 에 어느 칸을 어떻게 거르는지 담는다
  예: `{"column": "group_id", "op": "eq", "value": "BOND"}`
- **종목 표에 실제로 있는 칸만 쓴다** — 종목코드·이름·업종·자산군·위험등급.
  보수율·최대낙폭은 비어 있어서 지금은 거를 수 없다

**④ ETF 특성 `etf_trait` — 8개 내외**

- 종목 이름에서 읽히는 성격. 자연어의 "배당주로" / "환헤지 된 걸로" 에 대응한다
- 예: `ET_DIVIDEND` 배당 · `ET_COVERED_CALL` 커버드콜 · `ET_HEDGED` 환헤지(H) ·
  `ET_TR` 분배금 재투자 · `ET_ACTIVE` 액티브
- 판정은 종목명 문자열 규칙이다. `build_universe.py` 의 업종 분류와 같은 방식이고,
  거기 있는 정규식을 참고하되 **종목 표에 칸을 추가하지 않는다**

**⑤ 렌즈 `lens` — 보류**

- 감성 분석이 기사를 어느 관점으로 읽을지를 가리키는 블록이다
- 감성 점수 모델 자체가 아직 없고 렌즈 목록 정의도 없다. **M3 와 조율 전까지 비워 둔다**

### 태그 체계

- `bbl_tags` 는 `(블록, 태그)` 평면 표다. 계층도 이름공간도 표가 주지 않는다


**접두어 다섯 개로 고정한다. 늘리지 않는다.**

- `kw:` — 사용자 문장에서 바로 잡히는 한국어 말
  - `kw:배당` · `kw:안전하게` · `kw:반도체`
- `asset:` — 자산군
  - `asset:equity` · `asset:bond` · `asset:commodity`
- `sector:` — 업종 6개
  - `sector:semiconductor` · `sector:battery` · `sector:biohealth` ·
    `sector:finance` · `sector:internet_platform` · `sector:consumer`
- `country:` — 국가
  - `country:kr` · `country:us` · `country:other`
- `risk:` — 위험 성향 연관
  - `risk:low` · `risk:mid` · `risk:high`

**규칙**

- 접두어와 값은 전부 소문자. `kw:` 값만 한국어 원문 그대로 쓴다
- `asset:` · `sector:` · `country:` 의 값은 **`asset_groups.group_id` 를 소문자로
  바꾼 것**이어야 한다. 새로 지어내지 않는다 — 종목을 거를 때 바로 맞춰 쓸 수 있고,
  어긋나면 테스트가 잡는다
- 한 블록에 태그를 다섯 개 이상 달지 않는다. 다 달면 검색이 전부 걸려 소용없어진다
- `kw:` 는 **한 낱말**로 쓴다. "배당 잘 나오는" 같은 구절은 `kw:배당` 으로 줄인다

### 검색 함수

- `app/repositories/bbl.py`
- **시점 규약 대상이 아니다.** 가드 대상 세 표가 아니고, 블록은 시점 자산이 아니다
- 서명은 태그 목록과 블록 종류를 받아 블록 목록을 돌려주는 모양
- 벡터 검색 쿼리는 뉴스 유사기사 조회(`repositories/news.py`)를 베껴 쓰면 된다
- **정렬에 블록 식별자를 항상 tie-break 로 넣는다.** 같은 질의에 순서가 흔들리면
  같은 입력에 다른 전략서가 나온다
- 비활성 블록은 제외한다
- **태그 점수와 벡터 점수를 가중합하지 않는다.** 태그로 거르고 벡터로 정렬, 순서대로.
  가중치를 섞으면 튜닝 값이 늘고 "왜 이 블록이 뽑혔는가" 를 설명하기 어려워진다

### 임베딩은 나중에

- 블록이 35개 규모면 **태그 매칭만으로 거의 다 잡힌다**
- 칸(768차원)과 색인이 이미 만들어져 있어서 나중에 붙이는 비용이 스크립트 하나다
- 붙일 때는 뉴스 쪽이 쓰는 한국어 문장 임베딩 함수를 그대로 부른다 — 차원이 같고
  새 의존성이 없다
- **시드 SQL 에 벡터 값을 박지 말 것.** 설명 문구를 고치면 다시 만들 수 없다
- 켜는 기준: "태그로 안 잡히는 자연어" 사례가 실제로 나올 때

### 완료 기준

- 시드가 적재되고 블록 종류별 개수가 맞는다 (지표 13 · 리밸런싱 6 · 필터·특성 각 8 내외)
- 지표 블록의 식별자가 `features.py` 의 피처셋 및 `macro/codes.py` 의 코드와
  **한 글자도 다르지 않다** — 테스트로 고정한다
- `asset:` · `sector:` · `country:` 태그 값이 `asset_groups` 에 실제로 있는 묶음과 맞는다
  — 테스트로 고정한다
- 태그로 블록을 찾을 수 있고, **같은 질의에 항상 같은 순서로** 나온다
- 리밸런싱 블록의 `params_schema` 를 그대로 전략서 `rebalance` 자리에 넣으면
  계약 검사를 통과한다

### 막는 것

- **없다. 1·2단계와 독립이고 선행 안건과도 무관하다**
- 다만 렌즈 블록만 M3 조율 대기

## 4단계 — 의도 파악과 후보 선정

- **하는 일** (`m1-todo` §5 §6 §7)
  - 자연어 → 구조화된 의도 추출, 태그 추출, 종목 언급 판별
  - 종목 후보 선정 — 언급됐으면 매칭, 아니면 업종·자산군으로 추림
  - 허용 상한이 0인 종목 제거
  - 전략서 스키마에 **종목별 최소·최대 비중을 동적으로 주입**
- **재사용할 것**
  - 스키마 생성·export 함수와 생성된 스키마 파일이 이미 있음
  - **새로 만들 것은 "종목별 상하한 주입" 한 겹뿐.** 스키마를 손으로 다시 쓰면 계약이 두 벌이 됨
- **완료 기준**
  - 문장 하나를 넣으면 후보 종목 목록이 나오고, 각 종목에 성향별 상한이 붙음
  - 담을 수 없는 종목(상한 0)이 후보에서 빠짐
  - 같은 문장 두 번에 같은 후보가 나옴
- **선행 안건 ① 이 반드시 먼저.** 등급이 틀린 채로 만들면 결과를 보고 코드를 의심하게 됨

## 5단계 — 전략서 생성과 후처리

- **하는 일** (`m1-todo` §8 §9)
  - 컴파일 프롬프트 설계 (의도 + 블록 + 후보 종목 + 제약)
  - LLM 호출 — 동적 스키마를 넘겨 형식을 강제
  - 결과 파싱 → 허용범위로 접기 → 원출력과 조정 여부 보존 → 저장
- **주의**
  - **Ollama 는 형식 강제가 약해 스키마 위반이 실제로 난다.** 로컬 개발은 전부 Ollama 이므로 파싱이 유일한 방어선
  - 정식 실측은 **vLLM 쪽에서** 해야 함(기준 문서 지시). GPU 있는 팀원 협조 필요 — **10월**
  - 범위를 접는 계산은 `policy.py` 에 이미 있음. 다시 쓰지 말 것 (선행 안건 ③)
  - 저장 상태는 `draft` 까지만. 승인 상태가 되면 수정이 물리적으로 막힘
- **완료 기준**
  - 전략서가 형식 검사를 통과하고 데이터베이스에 저장됨
  - LLM 이 범위를 벗어난 비중을 냈을 때 접히고, **접었다는 사실과 원래 값이 함께 남음**
  - 같은 전략서를 M4 백테스트가 읽어서 돌릴 수 있음

## 6단계 — 파이프라인과 되묻기

- **하는 일** (`m1-todo` §10 §4)
  - 상태 머신 정의, 노드 연결, 되묻기 루프
  - `compile_spec` 작업의 속을 갈아끼움
- **격리 방침**
  - **닿는 접점은 한 군데.** 지금 3초 대기 후 끝나는 더미의 본체만 바꾼다
  - `app/m1/` 한 곳에 몰고 의존을 단방향으로 고정. 기존 모듈이 이쪽을 import 하지 않음
  - **작업 이름 `compile_spec` 은 바꾸지 않는다.** 라우터와 프론트가 이 이름에 붙어 있음
- **완료 기준**
  - Swagger 에서 요청 → 작업 번호 → 진행 상황 스트림으로 완료와 결과를 받음
  - 정보가 모자라면 되물어서 답을 받아 이어감
- **선행 안건 ② ④ 가 먼저**

## 7단계 — 샘플 10건

- **하는 일** (`m1-todo` §11)
  - 입력 문장 + 기대 전략서 쌍 10건
  - 전 구간 검증 테스트
- **10건에 반드시 넣을 것**
  - 성향별로 결과가 달라지는 쌍 — 같은 문장을 안정형과 공격형이 냈을 때
  - **실패해야 하는 입력 최소 2건** — 안정형에게 레버리지 요청, 상장폐지 종목 지정
    - 선행 안건 ⑤ 가 끝나야 만들 수 있음
  - 정보가 모자라 되묻게 되는 입력 1건
- **완료 기준**
  - 테스트가 CI 에서 돎. LLM 호출이 필요한 것은 `requires_ollama` 표시를 달아 기본 실행에서 뺌

---

## 범위 밖

- Validator 본체 — 담당 미정(선행 안건 ③ 과 함께 정리)
- 3관점 판단, 비중 매핑, 주문 생성 — M3·M4
- 백테스트 러너 — M4
- 화면 — M4
- 회원가입 이후의 권한 관리 — 지금은 일반 사용자로 통일

## 리스크

- **위험등급이 재배정되지 않으면 4단계부터 결과가 무의미해진다** — 가장 큰 위험
- **형식 강제 실측을 Ollama 로만 하면 vLLM 에서 다르게 동작할 수 있다** — 10월 안에 한 번은 GPU 환경에서 돌려봐야 함
- **표 구조를 또 바꿔야 하면 각자 데이터베이스를 날려야 한다** — 되묻기용 표를 추가할 경우 팀 전체에 영향. 안건 ② 를 미룰수록 비싸짐
