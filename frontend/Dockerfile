# TODO(9단계: Next.js 스캐폴드 완성 후 채움)
# 지금 단계(1~2)에서는 postgres/redis/minio만 기동 대상이라 이 이미지는 빌드하지 않는다.
FROM node:20-alpine
WORKDIR /app

# COPY package*.json ./
# RUN npm install
# COPY . .
# CMD ["npm", "run", "dev"]
