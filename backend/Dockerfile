# TODO(7단계: FastAPI 스켈레톤 완성 후 채움)
# 지금 단계(1~2)에서는 postgres/redis/minio만 기동 대상이라 이 이미지는 빌드하지 않는다.
FROM python:3.11-slim
WORKDIR /app

# COPY pyproject.toml ./
# RUN pip install --no-cache-dir -e .
# COPY app ./app
# CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
