"""파이썬 백테스트 러너.

lib/backtest.ts 와 같은 알고리즘을 파이썬으로 구현한 것이다. 실제 운용에서
백테스트는 Celery 워커 안에서 돌아야 하고(현서님 tasks.py 의 run_backtest),
신호를 내는 M3 모델도 파이썬이라 같은 프로세스에 있는 편이 낫다.

TS 구현은 버리지 않는다 — 두 구현이 같은 입력에 같은 주문을 내는지 대조하는
것이 이 프로젝트가 주장하는 재현성의 검증이다.

팀 리포로 옮길 때는 이 패키지를 backend/app/backtest/ 로 그대로 이동하면 된다.
"""
