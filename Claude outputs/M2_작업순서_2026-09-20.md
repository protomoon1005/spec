# M2 작업 순서 (2026-09-20 기준)

`M2_단계별_프롬프트`(9/15)를 대체합니다. 강준의 9/19·9/20 전달사항과 9/16 작업 결과를 반영했습니다.

**원칙: 데모 수준 핵심 기능만.** 데모계획 1.3이 11월 포함 범위와 12월 이후를 이미 갈라놨으니 그 선을 따릅니다.

---

## 오늘 (30분)

### 1. 푸시

```powershell
cd C:\Users\MHS\graduation-project\spec
git push
```

커밋 5개가 안 올라가 있습니다. 이것만으로 강준 문서의 세 항목이 닫힙니다.

### 2. 커버리지 검증

```
etf_master 71종목의 백테스트 구간(2023-01-02~2025-12-31) price_daily 커버리지를
검증해라. 데모 수준이니 간단히 간다.

종목별로 그 구간의 거래일 대비 실제 데이터 행 수 비율을 계산하고,
95% 미만인 종목을 목록으로 뽑아라.
단 active=false 인 상장폐지 3종목(248260/098560/301410)은 판정 대상에서 빼라.
구간 중 폐지가 정상이다.

미달 종목이 나오면 그 목록만 보고하고 멈춰라. 삭제하지 마라.
```

`0072R0 TIGER KRX금현물`(2025-06 상장)은 거의 확실히 걸립니다. `411060 ACE KRX금현물` 과 중복이라 빼도 손해 없습니다.

### 3. 강준에게 답장

`M2_답변_2026-09-20.md` 의 2절만 아래로 줄여서 보냅니다.

> 유니버스는 `db/seeds/04_etf_master.csv` 71종목으로 갑니다. 분류 4축이 이미 채워져 있고 FK 대상 테이블이 여기 걸립니다.
> `data/universe.csv` 의 순자산·커버리지 산정은 참고하겠습니다. 3년 커버리지 95% 필터는 제가 `price_daily` 로 직접 돌려 미달 종목을 빼겠습니다.
> `risk_tag` 만 상품 성격 기반으로 유지합니다 — 1절의 이유입니다. M4 복사본도 여기서 파생시켜 주시면 세 벌이 하나로 정리됩니다.

---

## 이번 주 전반 — 스키마 묶음 (남이 막혀 있는 것)

세 개가 한 덩어리입니다. **증분 SQL 경로가 없으면 나머지 둘을 못 합니다.**

### 4. 증분 SQL 경로 만들기

```
docs/infra-spec.md 와 plans/M2_요청사항_2026-09-19.md 1번, 9-20 문서 3번을 읽어라.

스키마 변경 경로를 만든다. alembic 복원이 아니라 증분 SQL 방식이다.
목적은 down -v 없이 스키마를 바꿀 수 있게 하는 것이지
완전한 마이그레이션 체계를 만드는 게 아니다. 데모 수준으로 얇게 간다.

만들 것:
- db/migrate/ 디렉터리. 파일명은 001_*.sql 처럼 3자리 순번 + 설명
- schema_migrations 테이블 (version, applied_at) — 적용 이력
- scripts/apply_migrations.py — 미적용 파일만 순서대로 실행하고 이력에 기록
- tasks.ps1 에 migrate 서브커맨드 연결 (기존 alembic 호출은 이걸로 교체)

db/init/ 은 빈 DB 부트스트랩 전용으로 남긴다. 건드리지 마라.

그리고 정리:
- db/migrations/__pycache__/ 와 versions/__pycache__/ 의 .pyc 5개 삭제
- .gitignore 에 __pycache__ 추가 (이미 있으면 확인만)
- docs/infra-spec.md 8장 수용 기준의 "Alembic 왕복" 항목을
  "db/migrate 순번 적용 + 재적용 멱등성" 으로 수정
- .github/workflows/ci.yml 에 마이그레이션 적용 검증 단계 추가
  (빈 DB → db/init → db/migrate 전부 적용 → 테이블·트리거 개수 확인)
```

검증: `.\tasks.ps1 down`, `up`, `migrate` 를 두 번 연속 돌려서 두 번째가 멱등인지 확인.

**커밋** — `feat(db): incremental SQL migration path`

### 5. etf_master 분류 3축 컬럼

```
db/migrate/001_etf_master_group_axes.sql 을 만들어라.

etf_master 에 축별 컬럼 3개를 추가한다.
- asset_group_id   VARCHAR REFERENCES asset_groups(group_id)
- sector_group_id  VARCHAR REFERENCES asset_groups(group_id)
- country_group_id VARCHAR REFERENCES asset_groups(group_id)

기존 group_id 는 당분간 자산군용으로 남긴다. 지우지 마라.

그 다음 scripts/load_etf_master.py 를 고쳐서
04_etf_master.csv 의 group_id / sector / country 값을
세 컬럼에 각각 매핑해 적재하게 해라.
CSV 의 sector 한글값(반도체·2차전지 등)과 country 한글값(한국·미국·기타)을
asset_groups 의 group_id(SECTOR_SEMICONDUCTOR, COUNTRY_KR 등)로 변환하는
매핑 테이블을 스크립트 안에 둔다.

country 컬럼 부재는 README "알려진 설계 구멍" ①번이었다. 이걸로 닫힌다고 기록해라.

적재 후 71행이 세 컬럼 전부 채워졌는지, FK 위반 0건인지 검증해라.
```

**커밋** — `feat(db): etf_master three classification axes`

### 6. status 전이 트리거 수정

```
db/migrate/002_spec_status_transition.sql 을 만들어라.

trg_strategy_specs_block_update 가 지금 status='approved' 인 행의 UPDATE를
무조건 막는데, status CHECK 제약은 draft → approved → running → closed 전이를
전제한다. 승인 이후로 한 발도 못 나가는 모순이다.

트리거를 status 컬럼을 제외한 나머지 컬럼에만 걸도록 고쳐라.
상세설계서 4.3 "Spec 불변" 의 의도는 유니버스·비중 범위 동결이고
status 는 그 대상이 아니라는 판단이다. 이 판단 근거를 SQL 주석에 남겨라.

spec_universe 의 트리거는 그대로 둔다 (전체 동결이 맞다).

검증: approved 상태에서
- status 만 running 으로 UPDATE → 성공해야 한다
- name 이나 input_prompt UPDATE → 여전히 거부돼야 한다
두 경우 다 테스트로 작성해라.

README "알려진 설계 구멍" 의 해당 항목을 해결됨으로 갱신해라.
```

**커밋** — `fix(db): allow status transition after approval`

---

## 이번 주 후반 — 가드와 문서 (짧은 것들)

### 7. as_of 가드 확장

강준이 `macro/codes.py:104` 문구를 고친 뒤에 합니다.

```
scripts/check_asof_guard.py 를 확장해라.

GUARDED_TABLES 에 price_daily, regime_snapshots 추가.
GUARDED_CLASSES 에 PriceDaily, RegimeSnapshots 추가.

확장 후 python scripts/check_asof_guard.py 를 돌려 위반 0건을 확인해라.
위반이 나오면 그 파일이 누구 소유인지 보고만 하고 고치지 마라.

test_check_asof_guard.py 에 새 테이블·클래스에 대한 테스트도 추가해라.
```

**커밋** — `feat(guard): extend as-of guard to price_daily and regime_snapshots`

### 8. 문서 정리

```
문서가 실제와 어긋난 곳을 고쳐라. 코드는 건드리지 마라.

1. docs/infra-spec.md 6장 "모듈 계약 4종" 을 5종으로 고친다.
   backend/app/contracts/integrated_signal.py 가 2026-09-12 팀 합의로 ⑤ 로 승격됐다.
   내용은 그 파일의 모델 정의와 docstring 을 읽어서 채워라.
   기획서 6.1 이 "계약 4종 동결, 변경 시 전원 합의" 라 한 항목이므로
   승격 일자와 경위를 각주로 남겨라.

2. docs/infra-spec.md 확정 기술 스택 표에서 실제로 안 쓰는 항목 정정:
   pykrx(KRX 계정 요구로 미사용), fredapi(ingest_macro.py 가 urllib 직접 호출),
   pandas-ta(features.py 가 직접 구현). 삭제하지 말고 "미사용" 으로 표시하고 사유를 적어라.

3. CLAUDE.md 43행 "ML extra 설치 시 --extra-index-url 필수" 삭제.
   torch 를 ml extra 에서 뺐으므로 더 이상 사실이 아니다.

4. docs/infra-spec.md 에 9장 "하지 말 것" 절이 없다.
   CLAUDE.md 가 이 절을 참조하고 있으므로, 해당 참조를 지우거나
   절을 복원해라. 복원한다면 지금 구현 범위 기준으로 다시 써라.

5. README "알려진 설계 구멍" 에서 risk_tag 문구를 나눠라.
   asset_group / sector_group / country 는 자동 산정 + 사람 검토 가능,
   risk_tag 는 상품 성격 기반으로 사람이 판단 (변동성 분위 산정 금지).
   근거는 상세설계서 1.7.2 — G1 = 레버리지·인버스 전제.

6. build_etf_master.py 를 삭제해라. 04_etf_master.csv 가 채워졌고
   pykrx 의존을 없앤다. delisted_candidates.csv 는 남긴다.
   README 의 해당 안내도 정리해라.
```

**커밋** — `docs: sync documentation with actual state`

---

## 다음 주 — 본체 (데모에 보이는 것)

### 9. Validator 4단 + 범위 자동 보정

`docs/m2-algorithms.md` 1장·6장이 정본입니다. **범위 자동 보정만 꼼꼼히, 나머지는 얇게.**

실패 장면 2가 이 기능의 시연이라 여기만 제대로 만듭니다.

### 10. 비중 산출 체인 마무리

하늘이 `backend/app/backtest/` 에 70%를 만들어놨습니다. 네 조각만 채웁니다.

- RiskSizer (ATR 상한) — 공식 하나, 단순하게
- OrderBuilder (가상 체결) — 목표비중 차이 → 수량, 그 이상 안 함
- 계약 ④ `TargetWeights` 형식 정합
- 1.5.3 검산 예제 회귀 테스트

프롬프트는 그때 드리겠습니다. 9번과 10번은 분량이 있어 각각 하루씩 잡으세요.

---

## 미루는 것

| 항목 | 언제 | 이유 |
|---|---|---|
| `run_id` 축 (9/19 7번) | 12월 이후 | 데모는 샘플 10건을 한 번씩 돌려 겹치지 않음 |
| 가드 docstring 오탐 완화 (9/19 6-2) | 데모 후 | 강준도 "급한 거 아님" |
| 정책 3중 사본 검사 (9/19 8번) | 여유 있을 때 | 테스트 하나면 충분 |
| 섹터 목록 확장 (9/20 7번) | 데모 후 | `SECTOR_OTHER` 면제로 당장은 해결 |
| 실패 장면 3종 | 11월 3~4주 | 데모계획 4장 |

## 남이 하는 것

- **Dockerfile `.[dev,ml]` + `libgomp1`** — 강준이 PR 올리면 승인. 단일 이미지 유지(9/12 합의)
- **`macro/codes.py` 문구 수정** — 강준. 7번의 선행 조건
- **`price_daily` 적재** — 완료 (52,543행)
